<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baomidou.ant.sys.mapper.UserMapper">
<!--UserMapper中的一个方法对应这里的一个SQL语句  Mapper接口和映射文件要保证一致：
    1. mapper接口的全类名称和映射文件的namespace一致
    2.mapper接口中的方法名和映射文件中sql的id保持一致
    -->

    <!--int insertUser();-->
    <!--
    <insert id="insertUser">
       insert into table_name value(a,b,c,d);
     </insert>
    -->

    <!--    List<Integer> getUserIdList();-->
    <!--    获取所有用户id列表-->
    <select id="getUserIdList" resultType="java.lang.Integer">
        select id from ;
    </select>

</mapper>
